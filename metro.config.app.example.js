// apps/app-werewolf-mafia/metro.config.js
// Copy this file into every app. Change nothing — __dirname does the work.

const { createMetroConfig } = require('../../metro.config.base');
module.exports = createMetroConfig(__dirname);
